import React, { Component } from 'react';
import PropTypes from 'prop-types';

const Weather = props => {
    return (
      <div id="weather">
        IM THE WEATHER
      </div>
    )
}

export default Weather
